<h2><%= @title %></h2>
<%= form_tag enemies_path, :method => 'get', :id => "enemies_search" do %>
<p>
  <b>Name:</b>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search", :name => nil %>
</p>
<div id="enemies">
  <%= render 'enemies' %>
</div>
<% end %>
<br />
<%= link_to 'Create new enemy', new_enemy_path if current_user.admin? %>
