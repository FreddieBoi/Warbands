<h2><%= @title %></h2>
<%= form_tag regions_path, :method => 'get', :id => "regions_search" do %>
<p>
  <b>Name:</b>
  <%= text_field_tag :search, params[:search] %>
  <%= submit_tag "Search", :name => nil %>
</p>
<div id="regions">
  <%= render 'regions' %>
</div>
<% end %>
<br />
<%= link_to 'Create new region', new_region_path if current_user.admin? %>
