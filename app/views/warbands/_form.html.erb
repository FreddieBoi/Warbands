<%= form_for(@warband) do |f| %>
  <% if @warband.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@warband.errors.count, "error") %> prohibited this warband from being saved:</h2>

      <ul>
      <% @warband.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :reputation %><br />
    <%= f.text_field :reputation %>
  </div>
  <div class="field">
    <%= f.label :pos_x %><br />
    <%= f.text_field :pos_x %>
  </div>
  <div class="field">
    <%= f.label :pos_y %><br />
    <%= f.text_field :pos_y %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
