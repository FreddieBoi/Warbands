<h2><%= @title %></h2>
<p>
  <%= @enemy.desc %>
</p>
<p>
  <b>Region:</b>
  <% if @region.blank? %>
  Currently not in any region.
  <% else %>
  <%= link_to @region.name, @region %>
  <%end %>
</p>
<p>
  <b>Starting health:</b>
  <span class="health"><%= @enemy.max_health %></span>
</p>
<h3>Equipped items</h3>
<% if @items.empty? %>
<p>
  This enemy doesn't have any items.
</p>
<% else %>
<ul class="items">
  <%= render :partial => "item_templates/item", :collection => @items %>
</ul>
<% end %>
<p>
  <em>Enemies might drop equiped items upon dying.</em>
</p>
<p>
  <% if current_user.admin? %>
  <%= link_to 'Edit', edit_enemy_template_path(@enemy) %> |
  <%= link_to 'Destroy', @enemy, :confirm => 'Are you sure?', :method => :delete %> |
  <% end %>
  <%= link_to 'List of all enemies', enemy_templates_path %>
</p>
<br>
<%= raw(disqus_thread) %>
